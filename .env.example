# Slack tokens — shared across the team, get from your team lead
SLACK_APP_TOKEN=xapp-1-...
SLACK_BOT_TOKEN=xoxb-...

# Asana Personal Access Token — create at https://app.asana.com/0/developer-console
ASANA_ACCESS_TOKEN=1/...

# Channel to monitor (without #)
SLACK_CHANNEL_NAME=workforce-planning-core-bugs

# Base workspace folder
TARGET_REPO_PATH=~/workspace

# Repos to investigate (comma-separated folder names under TARGET_REPO_PATH)
# Leave empty to search the entire workspace
INVESTIGATION_REPOS=hibob,workforce-planning

# AI agent mode: "claude" or "cursor"
AGENT_MODE=cursor

# Agent context files (comma-separated absolute paths to .cursorrules, .mdc, CLAUDE.md, .ai-context/*.md)
# These are injected into the AI agent's prompt so it follows repo conventions
# Run slack-bug-agent --setup to auto-discover these from your repos
AGENT_CONTEXT_FILES=
